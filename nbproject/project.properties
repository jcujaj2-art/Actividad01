annotation.processing.enabled=true
annotation.processing.enabled.in.editor=true
annotation.processing.run.all.processors=true
annotation.processing.processor.options=
application.title=VeterinariaApp
application.vendor=Josue
build.classes.dir=${build.dir}/classes
build.classes.excludes=**/*.java,**/*.form
build.dir=build
build.generated.dir=${build.dir}/generated
build.generated.sources.dir=${build.dir}/generated-sources
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
debug.classpath=    ${run.classpath}
debug.test.classpath=    ${run.test.classpath}
dist.dir=dist
dist.jar=${dist.dir}/VeterinariaApp.jar
dist.javadoc.dir=${dist.dir}/javadoc
endorsed.classpath=
excludes=
includes=**
jar.compress=false
javac.classpath=
javac.deprecation=false
javac.source=1.8
javac.target=1.8
javac.test.classpath=    ${javac.classpath}:    ${build.classes.dir}
javadoc.additionalparam=
javadoc.author=false
javadoc.encoding=${source.encoding}
javadoc.noindex=false
javadoc.nonavbar=false
javadoc.notree=false
javadoc.private=false
javadoc.splitindex=true
javadoc.use=true
javadoc.version=false
javadoc.windowtitle=
main.class=veterinariaapp.Main
manifest.file=manifest.mf
meta.inf.dir=${src.dir}/META-INF
platform.active=default_platform
run.classpath=    ${build.classes.dir}
run.test.classpath=    ${build.test.classes.dir}:    ${javac.test.classpath}
source.encoding=UTF-8
src.dir=src
test.src.dir=test
